{
  "name": "vivera-insights",
  "version": "1.0.0",
  "description": "Vivera Insights - Dashboard React + Vercel Functions",
  "type": "module",
  "scripts": {
    "build": "cd web && npm install && npm run build",
    "dev": "cd web && npm run dev"
  }
}
