apiVersion: v1
kind: Service
metadata:
  name: executive-os-service
  namespace: executive-os
  labels:
    app: executive-os
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: executive-os
